// Слаги, заведённые ОДНОВРЕМЕННО как группа каталога и как тип товара.
//
// Проблема, из-за которой файл появился (разбор головного спроса, 10.09.2026):
// `/products/group/<slug>` и `/products/type/<slug>` показывали один и тот же набор
// товаров и один и тот же категорийный лонгрид, то есть были полными дублями по
// построению. В выдаче они делили сигналы и мешали друг другу — по запросу
// «умный дом» группа стояла на 25,0, а тип на 27,8 в двух позициях от неё.
//
// Канонической выбрана ГРУППА, а не тип:
//   · группа живёт в меню и получает 7 внутренних ссылок с ключевых страниц,
//     тип — ровно одну (со страницы /categories);
//   · «Домофония» как группа шире: 72 товара против 53 у типа, потому что включает
//     внутренние мониторы и вызывные панели, и её H1 «Домофоны в Ташкенте» точнее
//     отвечает запросу «домофон» (590/мес), чем «Домофония»;
//   · лонгрид рендерится на обеих страницах, поэтому при склейке текст не теряется.
//
// Тип → группа. Редирект стоит в /products/type/[slug]/page.tsx ДО резолва имени,
// и эти адреса исключены из карты сайта (sitemapData.ts), чтобы не подавать в
// поисковики страницы, которые сами себя перенаправляют.
export const GROUP_CANONICAL: Record<string, string> = {
  domofoniya: "domofoniya",
  "ip-telefoniya": "ip-telefoniya",
  "umnyy-dom": "umnyy-dom",
};
