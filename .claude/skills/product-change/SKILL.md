---
name: product-change
description: Чек-лист изменения/добавления/удаления товара в каталоге satsolutions.uz. Использовать при любой правке товаров: название, описание, фото, цена, снятие с продажи, новый товар.
---

# Изменение товара — обязательный чек-лист

## При ЛЮБОМ изменении товара (правило из памяти, всегда)
1. **i18n**: обновить переводы в JSON-оверлеях для uz/en/tr/zh (productI18n.json — править ЛОКАЛЬНО в репо, на сервере не трогать). Имена в БД только RU; локализованный поиск идёт через productI18nIndex.
2. **SEO-поля**: seoTitle/seoDescription пересобрать под новое содержимое.
3. Проверить slug: если меняется — 301 со старого.

## Новый товар
- Фото: белый фон, высокое качество (пайплайн фото-батчей в истории; fal-ключ /root/.fal_key для генерации/обработки).
- Описание → перевод на 4 языка (пайплайн в памяти satsolutions-desc-translation: batch-скрипты, переведено 3063/3063 — новые товары догонять тем же способом).
- Категория из укрупнённой таксономии (42 типа, group-разделы); попадёт в sitemap автоматически.

## Снятие товара
- НЕ удалять молча: добавить slug в `apps/web/src/lib/removedProducts.ts` → middleware отдаст 308 на /catalog/<brand>.
- Убрать URL из sitemap (генерируется сам после исчезновения товара).
- Удалённые ранее товары (349+119) НЕ восстанавливать.

## Запись в прод-БД
- Через seed-скрипт на моделях (tsx) + scp картинок — паттерн в памяти satsolutions-prod-data-writes (кейс Uzum). Не сырым SQL в обход моделей.
- tsx: top-level await падает (cjs) — оборачивать в `async function main()`.

## После
- Деплой по скилу `deploy`; проверить страницу товара curl'ом с -A Mozilla на всех 5 локалях.
