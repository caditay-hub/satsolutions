node_modules
.next
dist
build
coverage
.turbo
.DS_Store
*.log
# Артефакты инкрементальной сборки TS — регенерируются при каждом build,
# трекать нельзя: грязнят git-дерево и провоцируют лишние стеши при деплое
*.tsbuildinfo
.env
.env.local
.env.*.local
# Локальные копии серверных .env с боевыми секретами — НИКОГДА не коммитить
.env.server
*.env.server

# Локальные служебные файлы установки: дамп БД, логи, SQL-фиксы — не в git
.setup/

# Выгрузки Search Console / временный SEO-анализ — не в git
.seo-analysis/

# Медиа-загрузки (рантайм-данные, бэкапятся отдельно — не в историю git)
apps/api/uploads/

# Персональные настройки Claude Code (машинно-специфичные пути, прокси Headroom) — не в git
.claude/settings.local.json


# Локальные секреты (ключи API) — НИКОГДА не коммитить
.secrets/
